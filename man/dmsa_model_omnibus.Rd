% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{dmsa_model_omnibus}
\alias{dmsa_model_omnibus}
\title{Omnibus across the score projections}
\usage{
dmsa_model_omnibus(
  scores,
  formula,
  data,
  term,
  block = NULL,
  score_col = "S",
  B = 1999,
  seed = NULL
)
}
\arguments{
\item{scores}{data.frame from \code{dmsa_scores()}.}

\item{formula}{Formula referring to the score by \code{score_col}.}

\item{data, block, B, seed}{As in \code{dmsa_model()}.}

\item{term}{Term to test.}

\item{score_col}{Column name the formula uses for the score.}
}
\value{
list: \code{p_by_flavour}, the omnibus \code{p}, and the fits.
}
\description{
Refits the same model once per projection, substituting each into the column
named \code{score_col}, and combines the p-values with the aggregated Cauchy
test. ACAT is valid under arbitrary dependence, so hedging across projections
costs no multiplicity - unlike a Bonferroni over arms, which is what the
old \code{min(1, 2*min(p))} gate amounted to.
}
