% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{dmsa_fit}
\alias{dmsa_fit}
\title{Fit, pool and permute a DMSA set test under a declared design}
\usage{
dmsa_fit(
  data,
  probes,
  alignment,
  design,
  method = c("expected", "fixed"),
  B = 999L,
  engine = c("auto", "lm", "lmer"),
  beta_input = TRUE,
  check = TRUE,
  seed = NULL
)
}
\arguments{
\item{data}{data.frame with one row per observation.}

\item{probes}{Character vector of methylation columns, in the SAME order as
the rows of \code{alignment}.}

\item{alignment}{A \code{dmsa_align()} result.}

\item{design}{A \code{dmsa_design()} (see \code{alpha_design()}).}

\item{method}{\code{"expected"} (certainty-weighted) or \code{"fixed"}.}

\item{B}{Permutations. \code{0} skips the permutation (descriptive only).}

\item{engine}{\code{"auto"} reports from \code{lmer} when the design
declares random effects and \pkg{lme4} is installed, otherwise
\code{lm}. The permutation null is ALWAYS built with the matched
\code{lm}, and the observed \code{lm} statistic is the one compared to it
- mixing estimators between observed and null is the classic way to
manufacture significance. The agreement between the two is returned.}

\item{beta_input}{If TRUE (default) \code{probes} hold beta values and are
converted to M; set FALSE if they are already M.}

\item{check}{Run \code{dmsa_check_design()} first (default TRUE).}

\item{seed}{Optional integer for reproducible permutation.}
}
\value{
An object of class \code{dmsa_fit}.
}
\description{
Fit, pool and permute a DMSA set test under a declared design
}
