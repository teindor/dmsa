% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_user.R
\name{dmsa_report}
\alias{dmsa_report}
\title{Run a declared frame and deliver figures, tables and a summary}
\usage{
dmsa_report(frame, progress = NULL, beep = NULL)
}
\arguments{
\item{frame}{a \code{\link{dmsa_frame}}.}

\item{progress}{Show a 0-100% progress bar. \code{NULL} (default) takes the
value declared on the frame, which itself defaults to \code{interactive()}.}

\item{beep}{Completion sound. \code{NULL} (default) takes the frame's value.
\code{TRUE} is \pkg{beepr} sound 8; a number picks another; \code{FALSE}
is silent. Without \pkg{beepr} installed this is a no-op.}
}
\value{
object of class \code{dmsa_report}: file paths and result tables,
  invisibly printable.
}
\description{
Call 2 of the two-call interface. Runs each requested level x outcome with
the locked statistics, applies correction ONLY within level-local families,
runs the always-on brain-bridge post-hoc on surviving probes, and writes
everything under \code{frame$outdir}.
}
