% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_model.R
\name{dmsa_plot_locus_gviz}
\alias{dmsa_plot_locus_gviz}
\title{The locus panel drawn by Gviz}
\usage{
dmsa_plot_locus_gviz(
  probes,
  gene = "",
  gene_model = NULL,
  chrom = NA,
  genome = "hg38",
  extra = list(),
  ideogram = FALSE,
  file = NULL,
  width = 1800,
  height = 1100,
  res = 190
)
}
\arguments{
\item{probes}{Data frame with \code{probe}, \code{pos}, \code{b}, and
optionally \code{d}, \code{p}, \code{chr} - the same frame
\code{dmsa_plot_locus()} takes.}

\item{gene}{Gene symbol, used for the title and the model lookup.}

\item{gene_model}{A \code{dmsa_gene_model}, or \code{TRUE} to fetch one.}

\item{chrom}{Chromosome, e.g. \code{"chr5"}. Taken from the model or the
probes when absent.}

\item{genome}{Genome label passed to Gviz, default \code{"hg38"}.}

\item{extra}{A list of further Gviz tracks to stack under the model.}

\item{ideogram}{Draw the cytoband ideogram. Needs network on first use, as
Gviz fetches the band table from UCSC.}

\item{file}{Optional PNG path.}

\item{width, height, res}{PNG geometry.}
}
\value{
Invisibly, the list of tracks plotted.
}
\description{
The same probes and the same gene model as \code{\link{dmsa_plot_locus}},
handed to \pkg{Gviz} instead of drawn here. Use this when you want what Gviz
gives and this package does not: a real cytoband ideogram, transcript
collapsing, and the ability to stack any other \code{GdObject} you already
build - CpG islands, ATAC peaks, ChIP tracks, conservation - in the same
coordinate frame.
}
\details{
Gviz, GenomicRanges and IRanges are Suggests, not dependencies. This function
errors with the install line if they are absent; everything else in the
package keeps working without them.
}
\examples{
\dontrun{
gm <- dmsa_gene_model("NR3C1")
dmsa_plot_locus_gviz(pr, gene = "NR3C1", gene_model = gm)
}
}
\seealso{
\code{\link{dmsa_plot_locus}} for the dependency-free panel.
}
