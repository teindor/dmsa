% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangulate.R
\name{dmsa_triangulate}
\alias{dmsa_triangulate}
\title{Three complementary lenses on the same family}
\usage{
dmsa_triangulate(
  M,
  data,
  rhs,
  term,
  units,
  alignment,
  block = NULL,
  B = 1999,
  winsor = 3,
  method = c("expected", "fixed"),
  correction = c("maxT", "minP"),
  weighting = c("combined", "reliability", "flat"),
  w_floor = 1.5,
  seed = NULL
)
}
\arguments{
\item{M}{Numeric matrix, samples x probes, methylation on the analysis scale
(probes are the responses).}

\item{data}{data.frame of predictors, rows aligned to \code{M}.}

\item{rhs}{Right-hand-side terms, e.g. \code{c("anx","avo","sex_c","chip_f")}
or with an interaction \code{c("anx*ace", ...)}.}

\item{term}{Design column to test, as \code{model.matrix} names it.}

\item{units}{Character vector, one entry per column of \code{M}, naming the
unit that column belongs to (typically the gene).}

\item{alignment}{\code{dmsa_align()} result covering \code{M}'s columns.}

\item{block}{Permutation block labels, one per row.}

\item{B}{Permutations.}

\item{winsor}{MADs for winsorisation; \code{NULL} to disable.}

\item{method}{Pooling method for \code{dmsa_test()}.}

\item{correction}{\code{"maxT"} (default; keeps the information advantage of
a unit with many concordant probes) or \code{"minP"}.}

\item{weighting}{Character. Probe weighting engine within a unit: \code{"combined"} (default) fuses the flat and reliability statistics on one shared permutation stream, \code{"flat"} weights every usable aligned probe equally, \code{"reliability"} weights each probe by its item-rest correlation with the rest of its unit. Weights are computed from methylation alone, so the permutation null is unaffected.}

\item{w_floor}{Numeric. Lower bound applied to reliability weights before normalisation, so a single poorly behaved probe cannot be driven to zero influence. Ignored when \code{weighting = "flat"}.}

\item{seed}{Optional integer.}
}
\value{
data.frame, one row per unit: the raw and family-adjusted p under
  each lens, the ACAT omnibus, the direction, and how many probes agreed.
}
\description{
Three complementary lenses on the same family
}
