% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moderate.R
\name{dmsa_moderate}
\alias{dmsa_moderate}
\title{Moderated DMSA (mDMSA)}
\usage{
dmsa_moderate(
  data,
  probes,
  alignment,
  outcome,
  moderator,
  design,
  method = c("expected", "fixed"),
  center = TRUE,
  scale_outcome = TRUE,
  B = 999L,
  engine = c("auto", "lm", "lmer"),
  beta_input = TRUE,
  check = TRUE,
  seed = NULL
)
}
\arguments{
\item{data}{data.frame.}

\item{probes, alignment}{as in \code{dmsa_fit()}.}

\item{outcome}{Character. The outcome column (a psychological measure).}

\item{moderator}{Character. The moderator column.}

\item{design}{A \code{dmsa_design()}. Its \code{focal} is ignored and
replaced by \code{score * moderator}; its \code{fixed}, \code{random},
\code{exchangeable} and \code{forbid} are used as declared.}

\item{method}{\code{"expected"} or \code{"fixed"} scoring.}

\item{center}{Center the moderator (default TRUE). Leaving a moderator
uncentered makes the product term collinear with its parents and the
simple slopes uninterpretable; the returned object records what was done.}

\item{scale_outcome}{Standardise the outcome (default TRUE).}

\item{B}{Permutations (Freedman-Lane: residuals of the no-interaction model
are permuted within exchangeable blocks, preserving the dependence).}

\item{engine}{as in \code{dmsa_fit()}.}

\item{beta_input}{TRUE if \code{probes} hold beta values.}

\item{check}{Run the design check (default TRUE).}

\item{seed}{Optional integer.}
}
\value{
An object of class \code{dmsa_moderate}.
}
\description{
Moderated DMSA (mDMSA)
}
