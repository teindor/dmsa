% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{dmsa_model}
\alias{dmsa_model}
\title{One model, block-permutation calibrated, for any term}
\usage{
dmsa_model(
  formula,
  data,
  term,
  block = NULL,
  B = 1999,
  seed = NULL,
  nulls = FALSE
)
}
\arguments{
\item{formula}{Model formula.}

\item{data}{data.frame.}

\item{term}{Name of the coefficient to test, exactly as \code{model.matrix}
names it (e.g. \code{"S:ACE"}).}

\item{block}{Block labels, one per row of \code{data} - the family or
cluster identifier. Rows are permuted only within blocks of EQUAL SIZE,
because concatenating a random order of unequal-size families is not a
valid relabelling.}

\item{B}{Permutations.}

\item{seed}{Optional integer.}

\item{nulls}{Optional list of pre-computed permutation null matrices, as returned by an earlier call on the same frame. Supplying it reuses the shared permutation stream instead of drawing a new one, which is what keeps lens, engine and level results jointly calibrated.}
}
\value{
An object of class \code{dmsa_model}.
}
\description{
The upper level of DMSA 2.0. Fits \code{formula} once, then permutes the
reduced-model residuals within permutation blocks of equal size
(Freedman-Lane) to obtain an exact p-value for one named coefficient -
including an interaction. \code{\link{dmsa_moderate}} is the special case of
this for \code{score * moderator} with a declared \code{dmsa_design()};
use that when you want the design contract enforced and simple slopes
reported, and this when the question is an arbitrary term.
}
