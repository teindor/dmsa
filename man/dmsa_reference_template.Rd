% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference_csv.R
\name{dmsa_reference_template}
\alias{dmsa_reference_template}
\title{Write a template csv for a user-supplied reference}
\usage{
dmsa_reference_template(path, with_modules = TRUE)
}
\arguments{
\item{path}{Where to write it.}

\item{with_modules}{Include the module column.}
}
\value{
\code{path}, invisibly.
}
\description{
Produces a small, filled-in example with every accepted column, so the shape
is unambiguous. Overwrite the rows with your own.
}
\examples{
f <- tempfile(fileext = ".csv")
dmsa_reference_template(f)
head(utils::read.csv(f), 3)
## the filled-in rows show every accepted column; replace them with your own
## panel, then read the result back as a reference bundle
dmsa_reference_csv(f, quiet = TRUE)
}
