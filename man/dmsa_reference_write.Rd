% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference_csv.R
\name{dmsa_reference_write}
\alias{dmsa_reference_write}
\title{Write a reference bundle to a directory}
\usage{
dmsa_reference_write(reference, dir)
}
\arguments{
\item{reference}{A \code{dmsa_reference}.}

\item{dir}{Directory to create and write into.}
}
\value{
\code{dir}, invisibly.
}
\description{
The inverse of \code{dmsa_reference_read()}: useful for saving a bundle built
from a one-file csv, or a generated bundle you have edited.
}
\examples{
f <- tempfile(fileext = ".csv")
dmsa_reference_template(f)
ref <- dmsa_reference_csv(f, quiet = TRUE)

d <- tempfile()
dmsa_reference_write(ref, d)
list.files(d)
nrow(dmsa_reference_read(d)$systems)
}
