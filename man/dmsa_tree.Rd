% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{dmsa_tree}
\alias{dmsa_tree}
\title{Build a cascade tree for a set of probes from a reference bundle}
\usage{
dmsa_tree(reference, genes, system_id = NULL, levels = NULL)
}
\arguments{
\item{reference}{A \code{dmsa_reference}.}

\item{genes}{Character vector, the set-membership gene of each probe.}

\item{system_id}{Optional: restrict to one system (a gene can sit in several,
which would otherwise duplicate probes).}

\item{levels}{Which levels to include, in order. Defaults to
\code{c("system", "module")} when a module layer exists, else
\code{"system"}. \code{"gene"} is always appended.}
}
\value{
data.frame with one row per probe. Probes whose gene is not in the
  reference get \code{NA} and should be routed to the flat unannotated arm.
}
\description{
Returns the \code{tree} argument \code{dmsa_cascade()} expects: one row per
probe, columns ordered outermost first.
}
