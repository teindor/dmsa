% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_model.R
\name{dmsa_gene_model}
\alias{dmsa_gene_model}
\title{A gene's exon, intron and UTR structure on real coordinates}
\usage{
dmsa_gene_model(
  gene,
  source = c("auto", "ensembl", "gff", "txdb", "ensdb", "table"),
  file = NULL,
  db = NULL,
  orgdb = NULL,
  table = NULL,
  species = "homo_sapiens",
  genome = "hg38",
  cache = NULL,
  host = "https://rest.ensembl.org",
  quiet = FALSE
)
}
\arguments{
\item{gene}{Gene symbol, e.g. \code{"NR3C1"}.}

\item{source}{One of \code{"auto"}, \code{"ensembl"}, \code{"gff"},
\code{"txdb"}, \code{"ensdb"}, \code{"table"}. \code{"auto"} takes a cached
copy if there is one, then Ensembl.}

\item{file}{GTF/GFF3 path when \code{source = "gff"}.}

\item{db}{TxDb or EnsDb object when \code{source} is \code{"txdb"} /
\code{"ensdb"}.}

\item{orgdb}{OrgDb for symbol lookup with \code{"txdb"} (e.g.
\code{org.Hs.eg.db}).}

\item{table}{A data frame already in this shape, when \code{source =
"table"}.}

\item{species}{Ensembl species name. Default \code{"homo_sapiens"}.}

\item{genome}{Genome label recorded on every row. Default \code{"hg38"} -
the assembly the bundled Alpha coordinates use.}

\item{cache}{Directory for fetched models, or \code{NULL} to disable.
Default is a \code{dmsa} folder under the session temp directory.}

\item{host}{Ensembl REST host. \code{NULL} disables the network entirely and
returns an empty model, which is how you guarantee an offline build.}

\item{quiet}{Suppress progress messages.}
}
\value{
A data frame with the columns in \code{.GM_COLS}, class
  \code{dmsa_gene_model}. Zero rows if the model could not be obtained -
  never a fabricated one.
}
\description{
Returns one row per drawable feature - coding exon slice, 5' UTR, 3' UTR, or
an exon whose coding status the annotation did not resolve - for every
transcript of a gene, with strand and genomic start/end. This is what
\code{\link{dmsa_plot_locus}} draws the gene from, and it is deliberately a
plain data frame so it can be built, inspected, corrected and cached like
any other reference layer in this package.
}
\examples{
\dontrun{
## Needs network: one lookup plus one region query per gene.
gm <- dmsa_gene_model("NR3C1")
subset(gm, canonical)[, c("feature", "start", "end", "exon_rank")]

## Or from an annotation you already have, with no network at all:
gm <- dmsa_gene_model("NR3C1", source = "gff", file = "Homo_sapiens.gtf.gz")
}
}
\seealso{
\code{\link{dmsa_gene_model_check}}, \code{\link{dmsa_plot_locus}}
}
