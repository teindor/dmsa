% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gate.R
\name{dmsa_gate}
\alias{dmsa_gate}
\title{Gated hierarchical DMSA: one score, one model, level-local correction}
\usage{
dmsa_gate(
  data,
  M,
  map,
  alignment,
  formula,
  term,
  roots,
  block = NULL,
  alpha = 0.05,
  B = 1999,
  winsor = 3,
  gate = c("both", "sparse", "dense"),
  sparse_reach = "children",
  seed = NULL
)
}
\arguments{
\item{data}{data.frame with outcome, moderators and covariates.}

\item{M}{Numeric matrix, samples x probes, rows aligned to \code{data}.}

\item{map}{data.frame, one row per column of \code{M}, with the level columns
ordered outermost first, e.g. \code{system}, \code{module}, \code{gene},
\code{probe}. The innermost column is the probe level.}

\item{alignment}{\code{dmsa_align()} result for \code{M}'s columns in order,
or a numeric multiplier vector.}

\item{formula}{Upper-level model formula, using the literal name \code{S}
where the unit's score belongs.}

\item{term}{Coefficient to test, as \code{model.matrix} names it - e.g.
\code{"S"} or \code{"S:ACE"}.}

\item{roots}{Units at the outermost level that were PRE-REGISTERED. Their
count is the first family's size; one root means no correction at the top.}

\item{block}{Permutation block labels (family/cluster id), one per row.}

\item{alpha}{Level for every family, propagated to children that pass.}

\item{B}{Permutations.}

\item{winsor}{Passed to \code{dmsa_scores()}.}

\item{gate}{Logical. If \code{TRUE} (default) a level is entered only after the level above it has produced a surviving unit, which is the level-local cascade; if \code{FALSE} every declared level is tested independently.}

\item{sparse_reach}{Integer. Maximum number of units carried forward from a level into the level below it, used to keep a wide family from expanding the cascade without bound.}

\item{seed}{Optional integer.}
}
\value{
Object of class \code{dmsa_gate}.
}
\description{
Gated hierarchical DMSA: one score, one model, level-local correction
}
