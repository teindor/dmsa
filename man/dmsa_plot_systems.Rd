% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{dmsa_plot_systems}
\alias{dmsa_plot_systems}
\title{Panel 1: dense arm against sparse arm, every system}
\usage{
dmsa_plot_systems(
  systems,
  alpha = 0.05,
  file = NULL,
  width = 1500,
  height = 1400,
  res = 190
)
}
\arguments{
\item{systems}{data.frame with columns \code{system}, \code{outcome},
\code{p_dense}, \code{p_sparse}; optionally \code{primary} (logical) and
\code{top_gene}.}

\item{alpha}{Gate level. The gate is \code{min(1, 2*min(p_dense, p_sparse))},
so the boundary drawn is at \code{alpha/2} on each arm.}

\item{file}{Optional png path. If \code{NULL} draws to the current device.}

\item{width, height, res}{png dimensions.}
}
\value{
Invisibly, the plotted data frame.
}
\description{
Distance from the diagonal is how concentrated a system's response is: a
system null on the pooled arm and strong on the max-gene arm carries its
signal in one gene.
}
