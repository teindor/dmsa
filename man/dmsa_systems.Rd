% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cascade_sets.R
\name{dmsa_systems}
\alias{dmsa_systems}
\title{The systems available in a cascade, with the short names to select them by}
\usage{
dmsa_systems(x = "alpha", pattern = NULL)
}
\arguments{
\item{x}{A \code{dmsa_sets} or \code{dmsa_selection}, or anything
\code{dmsa_sets()} accepts. A bare string that is neither \code{"alpha"}
nor an existing file path is treated as \code{pattern}, so
\code{dmsa_systems("hpa")} searches the bundled cascade.}

\item{pattern}{Optional case-insensitive regular expression; matched against
the short name, the full system name and the system id.}
}
\value{
A data.frame with \code{system_id}, \code{system_short},
  \code{system} and the module/gene/CpG counts. Printed as a table.
}
\description{
The systems available in a cascade, with the short names to select them by
}
\examples{
\donttest{
dmsa_systems()             # all 30
dmsa_systems("stress|hpa") # search
}
}
