% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{dmsa_plot_genes}
\alias{dmsa_plot_genes}
\title{Panel 2: gene level inside one system}
\usage{
dmsa_plot_genes(
  genes,
  null_max = NULL,
  title = "",
  alpha = 0.05,
  file = NULL,
  width = 1500,
  height = 1500,
  res = 190
)
}
\arguments{
\item{genes}{data.frame for ONE system and outcome: \code{gene}, \code{z},
\code{n_probes}, optionally \code{p_adj} (max-T adjusted) and
\code{selected}.}

\item{null_max}{Optional numeric vector of the permutation null of
\code{max|z|} over the same genes; its .95 and .99 quantiles are drawn as
the multiplicity-adjusted thresholds.}

\item{title}{System label.}

\item{alpha}{Gate level. The gate is \code{min(1, 2*min(p_dense, p_sparse))},
so the boundary drawn is at \code{alpha/2} on each arm.}

\item{file}{Optional png path. If \code{NULL} draws to the current device.}

\item{width, height, res}{png dimensions.}
}
\value{
Called for its side effect of drawing a plot on the active
  graphics device. Returns \code{NULL} invisibly.
}
\description{
Panel 2: gene level inside one system
}
\examples{
set.seed(1)
genes <- data.frame(gene = paste0("G", 1:8), n_probes = sample(3:9, 8, TRUE),
                    z = c(rnorm(7), 3.6))
genes$p_adj <- 2 * pnorm(-abs(genes$z))
## the permutation null of max|z| over this same family of genes
null_max <- apply(matrix(rnorm(200 * 8), 200, 8), 1, function(r) max(abs(r)))
dmsa_plot_genes(genes, null_max = null_max, title = "HPA axis",
                file = tempfile(fileext = ".png"))
}
