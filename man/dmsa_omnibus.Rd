% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical.R
\name{dmsa_omnibus}
\alias{dmsa_omnibus}
\title{Directionless omnibus test of a categorical focal exposure over a set}
\usage{
dmsa_omnibus(data, Y, design, beta_input = TRUE, B = 999, seed = 1L)
}
\arguments{
\item{data}{Analysis data frame.}

\item{Y}{Probe matrix (subjects x probes), betas or M-values.}

\item{design}{A \code{dmsa_design} whose \code{focal_test} names the factor.}

\item{beta_input}{Passed through: are the columns of \code{Y} betas?}

\item{B}{Permutations.}

\item{seed}{RNG seed.}
}
\value{
A list with the pooled statistic, the permutation p-value, and an
  explicit \code{directional = FALSE}.
}
\description{
Pools the per-probe F statistic for the \code{k - 1} contrast columns and
calibrates it by the same block permutation the directional tests use. This
answers "does this factor move the set at all" and \strong{gives up the
direction}, which is the property that separates DMSA from the methods it is
compared against. Use it as a screen, or when the levels have no ordering and
no reference level is defensible - and report \code{dmsa_contrasts()}
alongside it.
}
